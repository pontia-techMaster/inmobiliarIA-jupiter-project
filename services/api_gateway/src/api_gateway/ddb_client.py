"""DynamoDB client for user data.

Intentionally empty for now — the ``/users`` endpoint returns stub data. A
real boto3-backed client will land here when the user-data flow is factored
out into its own service.
"""
